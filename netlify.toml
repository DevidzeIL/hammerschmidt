[build]
  command = "pnpm build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "latest"

# Плагин @netlify/plugin-nextjs автоматически обрабатывает Next.js деплой
